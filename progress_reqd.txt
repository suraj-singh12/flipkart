1st page
1) improve responsiveness
2) instead of removing options, make a new side se nikalne wala slider 
use bootstrap more to improve responsiveness

2nd page
1) change the names of classes, and custom class should have custom- in starting
2) also don't alter actual classes, make custom-


check the dynamic dropdown using js
D:\Coding_Related\full-stack-notes\dynamic_dropdown

your js data is going to be api, so it will be called in the way it is called in this drop.js
so you will have to make a no of objects of key, value pair for each category and won't need to get any external file access,
because your files will be apis and you will fetch data from them.
